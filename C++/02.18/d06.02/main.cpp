#include <iostream>
#include "list.cpp"


/*

*/
int main()
{
	/*
	int a;

	Node* head = new Node;

	head->value = 1;
	head->id = generateId();
	while (true)
	{
		std::cout << "Evvele 1\n";
		std::cout << "Axira 2\n";
		std::cin >> a;

		if (a == 1)
		{
			std::cout << "Qiymetini daxil et\n";
			std::cin >> a;
			first(head, a);
		}
		else if (a == 2)
		{
			std::cout << "Qiymetini daxil et\n";
			std::cin >> a;

			last(head, a);
		}
		std::cout << "\n\n\n";
		print(head);
	}
	*/
	system("pause");
	return 0;
}